requests>=2.31
PyYAML>=6.0
openpyxl>=3.1
rapidfuzz>=3.0

# The Google Sheets integration is optional and its client libraries are not
# needed to run any sponsorscan command. Install them only if you wire up an
# uploader:  pip install -r requirements-sheets.txt
